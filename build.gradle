plugins {
    id 'java-library' //какой язык используем
}

repositories {
    mavenCentral()
}

dependencies { //зависимости
    testImplementation(
            "com.codeborne:selenide:7.0.0",
            "org.junit.jupiter:junit-jupiter:5.10.0",
            'org.slf4j:slf4j-simple:2.0.7'
    )
}

test {
    useJUnitPlatform()
}